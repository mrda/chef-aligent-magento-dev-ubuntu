[Unit]
Description=Create templated configs
After=<%= default['app']['templates']['after'] %>
Before=<%= default['app']['templates']['before'] %>

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/templates.sh

[Install]
WantedBy=multi-user.target
